<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopcart Reset Password</title>
    <!-- favicon -->
    <link rel="icon" href="https://i.imgur.com/SkliARr.png" type="image/x-icon">
    <link rel="shortcut icon" href="https://i.imgur.com/SkliARr.png" type="image/x-icon">

    <!-- Bootstrap CSS Link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <!-- CSS Link -->
    <link href="../style.css" rel="stylesheet">
    </link>
    <!-- Font awesome link -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
        integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <style>
        body {
            overflow-x: hidden;
        }

        .password-input {
            position: relative;
            display: flex;
            align-items: center;
        }

        .password-field {
            padding-right: 30px;
            /* Add space for the icon */
        }

        .toggle-icon {
            position: absolute;
            right: 10px;
            cursor: pointer;
        }

        .logo {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }
    </style>
</head>

<body>
    <% if (alert=="Yes" ) { %>
        <script>alert("Password reset successfully!")</script>
        <script>window.location.href = "/login"</script>
        <% } %>
            <div class="container-fluid my-3 mt-0 p-0 mb-5">
                <nav class="navbar navbar-expand-lg navbar-light bg-info p-1">
                    <div class="container-fluid pl-0">
                        <div style="width:250px; height:60px;" class="d-flex">
                            <a href="../"><img src="https://i.imgur.com/SkliARr.png" alt="" class="logo"></a>
                            <a href="../" class="d-flex align-items-center text-decoration-none">
                                <h3 class="text-center fw-2 text-warning pt-1 title ps-0">Shopcart</h3>
                            </a>
                        </div>
                    </div>
                </nav>
                <h2 class="text-center fs-1 fw-bold text-dark pt-3 mt-3">Reset Password</h2>
                <div class="row d-flex justify-content-center mt-3">
                    <div class="lg-12 col-xl-6">

                        <div class="bg-danger text-light fw-bold text-center">
                            <% if (alert=="No" ) { %>
                                <p>Password Do not Set! Try Again</p>
                                <% } %>

                        </div>
                        <!-- Username -->
                        <form action="" method="post" style="background-color:#eee;border-radius:15px;"
                            class="px-5 py-4">
                            <!-- Password Field -->
                            <div class="form-outline mb-3">
                                <label for="user_password" class="form-label mb-0 fw-bold">Password</label>
                                <div class="password-input">
                                    <input type="password" id="user_password" name="user_password" class="form-control"
                                        placeholder="Enter Password" autocomplete="off" value="<%= password %>">
                                    <span class="toggle-icon" id="toggleIcon" onclick="togglePassword()">
                                        <i class="fas fa-eye-slash"></i>
                                    </span>
                                </div>
                                <span class="text-danger fw-bold" style="font-size:10px;">
                                    <%= pass_error %>
                                </span>
                            </div>
                            <script>
                                function togglePassword() {
                                    var passwordField = document.getElementById("user_password");
                                    var toggleIcon = document.getElementById("toggleIcon");

                                    if (passwordField.type == "password") {
                                        passwordField.type = "text";
                                        toggleIcon.innerHTML = '<i class="fas fa-eye"></i>';
                                    } else {
                                        passwordField.type = "password";
                                        toggleIcon.innerHTML = '<i class="fas fa-eye-slash"></i>';
                                    }
                                }
                            </script>

                            <!-- Confirm Password Field -->
                            <div class="form-outline mb-3">
                                <label for="conf_user_pass" class="form-label   mb-0 fw-bold">Confirm Password</label>
                                <div class="password-input">
                                    <input type="password" id="conf_user_pass" name="conf_user_pass"
                                        class="form-control" placeholder="Confirm Password" autocomplete="off"
                                        value="<%= confPassword %>">
                                    <span class="toggle-icon" id="toggleConfIcon" onclick="toggleConfPassword()">
                                        <i class="fas fa-eye-slash"></i>
                                    </span>
                                </div>
                                <span class="text-danger fw-bold" style="font-size:10px;">
                                    <%= conf_pass_error %>
                                </span>
                            </div>
                            <script>
                                function toggleConfPassword() {
                                    var passwordField = document.getElementById("conf_user_pass");
                                    var toggleIcon = document.getElementById("toggleConfIcon");

                                    if (passwordField.type == "password") {
                                        passwordField.type = "text";
                                        toggleIcon.innerHTML = '<i class="fas fa-eye"></i>';
                                    } else {
                                        passwordField.type = "password";
                                        toggleIcon.innerHTML = '<i class="fas fa-eye-slash"></i>';
                                    }
                                }
                            </script>
                            <!-- Buttons -->
                            <div class="mt-4 pt-2">
                                <input type="submit" value="Reset Password"
                                    class="bg-info py-2 px-4 border-0 rounded text-light fw-bold" name="reset_pass">
                            </div>
                        </form>
                    </div>
                </div>
            </div>
</body>

</html>